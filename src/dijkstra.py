# class Dijkstra:
#     def __init__(self, graph):
#         self.graph = graph
    
#     def shortest_path(self, start):
#         distances = {}
#         for zone_name in self.graph.zones:
#             distances[zone_name] = float("inf")
#             start_zone = list(distances.keys())[0]
#             distances[start_zone] = 0
#         return distances
